using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ball : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
        private int side = Random.Range(0,2);

        if(side == 0){
            //Go left
        }

        else{
            //Go right
        }

    }


}
